import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.frugalp.horometros',
  appName: 'FRUGALP',
  webDir: 'dist/frontend-horometros/browser'
};

export default config;
